// Svelte 5: runes mode is used explicitly (see src/DocxViewer.svelte and the
// .svelte.ts composable), so no legacy `export let` props are involved.
export default {
  compilerOptions: {
    runes: true,
  },
}
